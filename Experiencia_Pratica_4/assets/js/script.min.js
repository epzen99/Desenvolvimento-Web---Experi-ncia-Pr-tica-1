const menuToggle=document.querySelector(".menu-toggle"),menuLista=document.querySelector(".menu-lista");menuToggle.addEventListener("click",()=>{menuLista.classList.toggle("ativo")}),document.querySelectorAll(".menu-lista a").forEach(e=>{e.addEventListener("click",()=>{menuLista.classList.remove("ativo")})});